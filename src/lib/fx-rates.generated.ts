/* AUTO-GENERATED by scripts/fetch-fx-rates.ts from the fx_rates table.
   Do NOT edit by hand.

   Build-time mirror of the live FX rates. country.ts's USD_FX reads this so
   the visitor-currency display map stays a STATIC, client-bundle-safe
   constant that renders identically on the server and the client (no
   hydration drift from a per-request DB read). The daily FX cron rewrites
   and commits this file when rates move. "1 USD = X local".

   Seeded with the previous hardcoded values so the read-path change is
   behaviour-neutral on deploy; the first cron run refreshes them to live. */
export const FX_GENERATED: Record<string, number> = {
  USD: 1,
  NGN: 1600,
  GBP: 0.79,
  EUR: 0.92,
  AED: 3.67,
  INR: 83,
  ZAR: 18.5,
};
